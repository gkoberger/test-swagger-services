openapi: 3.0.0
info:
    version: 0.0.1
    title: temp
servers:
    - {url: 'https://test123.com'}
paths:
    /pets: {get: {description: 'Returns all pets from the system that the user has access to', produces: [application/json], responses: {'200': {description: 'A list of pets.'}}}}
